#ifndef COMMON_H
#define COMMON_H

#include <stdbool.h>

// common.h, a module created for basic tasks.

// str_equ(a, b) returns true if the strings are equal and false otherwise,
//   ignoring letter casing
bool str_equ(char *a, char *b);

#endif